# =============================================================================
# APIGuard Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your environment.
# Do NOT commit your .env file to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------

# Port the APIGuard API server listens on.
APIGUARD_PORT=8080

# Bind address for the API server.
APIGUARD_HOST=0.0.0.0

# Log verbosity level: debug, info, warn, error.
APIGUARD_LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------

# PostgreSQL connection string.
# Format: postgres://user:password@host:port/dbname?sslmode=disable
APIGUARD_DB_URL=postgres://apiguard:changeme@localhost:5432/apiguard?sslmode=disable

# -----------------------------------------------------------------------------
# Redis
# -----------------------------------------------------------------------------

# Redis connection string used for caching and job queues.
APIGUARD_REDIS_URL=redis://localhost:6379/0

# -----------------------------------------------------------------------------
# Auth
# -----------------------------------------------------------------------------

# Secret key used to sign JWT tokens. Must be at least 32 characters.
# Generate one with: openssl rand -base64 48
APIGUARD_JWT_SECRET=change-me-to-a-random-string-at-least-32-chars

# Duration before issued JWT tokens expire (e.g., 1h, 24h, 7d).
APIGUARD_JWT_EXPIRY=24h

# -----------------------------------------------------------------------------
# Scanner
# -----------------------------------------------------------------------------

# Maximum duration for a single scan operation.
APIGUARD_SCAN_TIMEOUT=30s

# Number of concurrent scan workers.
APIGUARD_SCAN_CONCURRENCY=10

# Maximum allowed OpenAPI spec file size in megabytes.
APIGUARD_MAX_SPEC_SIZE_MB=10

# -----------------------------------------------------------------------------
# Report
# -----------------------------------------------------------------------------

# Directory where generated scan reports are stored.
APIGUARD_REPORT_DIR=./reports

# Default report output format: json, html, sarif.
APIGUARD_DEFAULT_FORMAT=json

# -----------------------------------------------------------------------------
# Dashboard
# -----------------------------------------------------------------------------

# Port the React dashboard dev server listens on.
APIGUARD_DASHBOARD_PORT=3000

# -----------------------------------------------------------------------------
# Citadel Integration (optional)
# -----------------------------------------------------------------------------
# Enable integration with Citadel for centralized policy management.

# APIGUARD_CITADEL_ENABLED=false
# APIGUARD_CITADEL_URL=http://localhost:8069
# APIGUARD_CITADEL_TOKEN=
