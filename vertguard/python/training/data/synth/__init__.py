"""Synthetic data generators for the VertGuard prompt-injection corpus.

These generators are deterministic (seeded RNG) so output is reproducible
across machines. Output is labelled `source="synthetic:<generator>:<version>"`
so it is never confused with samples pulled from real datasets.
"""
